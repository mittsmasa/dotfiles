sourceDir = {{ .chezmoi.sourceDir | quote }}

[data]
    name = {{ .name | quote }}
    email = {{ .email | quote }}
{{- if eq .chezmoi.os "darwin" }}
    signingkey = {{ .signingkey | quote }}
{{- end }}
